> **Scope update 2026-09-15:** Raster rows below describe historical experiments.
> Active FieldGrid v0.2.3 uses GPS and local motion assistance, with no recording,
> phone API or sync. [Current evidence](wrist-motion/README.md): 116 Python
> tests, 37 watch tests, zero warning fr165 build and physical USB readback PASS.
> Prior v0.2.2 stationary drift FAIL by user report; v0.2.3 stationary hold and walking
> response PASS by user report. Battery and full G0/G3 remain NOT RUN.

# G0 capability and evidence report

14 September 2026. **Physical G0 gate: PENDING.** The user requested simulator only
work and subsequently authorized the real map/English/offline coordinate extension.
PASS below applies only to the stated local observation.

| Check | Current local evidence | Physical watch / iPhone |
|---|---|---|
| Toolchain | SDK 9.2.0, `fr165`, 390×390, Java 11; compile PASS, zero warnings | Device XML SoftwareVersion 2905 read during sideload |
| Python | 89 tests PASS; API 96% line coverage | — |
| Monkey C | 22 tests PASS; current local source/PRG hashes in `compact-attribution/result.json` | Corrected PRG sideload/readback PASS; user reports map working without crash |
| English UI | Home, coordinates, fix loss/recovery, day/night, credits visually checked; measured round screen width tests PASS | Readability NOT RUN |
| Offline coordinates | BLE Not Connected + API absent; current synthetic coordinates/age, Images 0, advancing GPS events PASS | User reports GPS fix; independent accuracy/age NOT RUN |
| OpenFreeMap renderer | Six size/theme profiles and two additional cities PASS locally; public HTTPS preflight PASS | Initial display FAIL (drawBitmap2 crash); corrected display PASS by user report and successful new downloads |
| RAM stress | 40 sessions, 15,000 GPS events, 160 bitmap replacements; bounded trace/cleanup PASS | Native graphics / endurance NOT RUN |
| Storage | One small preference key, deduplicated writes; no map/GPS database | Capacity, bitmap, reboot NOT RUN |
| iPhone/GCM | Previous synthetic HTTPS simulator bridge only | Short locked phone refresh and UP/DOWN zoom PASS by user report; 30-minute endurance NOT RUN |
| Battery | No physical reference run | NOT RUN |

## Current resource evidence

The corrected 22-test run recorded **47,384 B post stop application heap** and
**66,288 B peak while old and incoming bitmaps overlapped**. After the measurement,
logging allocated 112 B. Four bitmap replacements per session cycle through
195/256/390 px with the 16-color download palette and conversion into native color;
there are 40 sessions. The trace never
exceeds 180 points. Stop clears image, trace, coordinates, timer, subscription and
network job. The first session feeds 7,200 one second event timestamps rapidly;
it is not two hours of elapsed runtime. These figures cannot establish the
physical graphics heap, long term battery use or NFR acceptance.

Offline GUI diagnostics showed Phone NO, Images 0, GPS 204 and Trace 83 with live
coordinate age 0 seconds; the native saved screenshot was taken shortly afterward.
API and HTTPS tunnel were not running. All GPS input was an explicitly loaded
synthetic Utrecht GPX, not a physical/user position. English remained active while
the simulator's system language was Turkish. The session ended on the home screen.

Earlier direct render Utrecht PNG samples: 195 day/night 8,429/8,410 B; 256 day/night
11,566/11,541 B; 390 day/night 21,770/21,790 B. Fixed Amsterdam and Brussels
390 px samples were 29,274 and 35,273 B. Individual local renders took approximately
0.84–2.12 seconds; these are samples, not p95 or Garmin transfer latency. The
provider's free service has no SLA. Dense tiles above 1 MiB are rejected explicitly.

Watch RAM limits: 180 trace points; one active native raster, one incoming native
raster plus its paletted source during conversion; one map job;
96 KiB free heap reserve before requesting. No persisted GPS history or map cache.
Server limits: 9 viewport tiles, 1 MiB each, 16 entries AND 8 MiB compressed cache,
one decoded tile at a time, one render at a time, 18-second deadline, 64 KiB output,
24 output entries. These are separate server budgets, not watch storage.

The final real HTTP smoke test used a fixed Utrecht z15 view: six profiles PASS,
10,756–32,856 B, 1.32–1.97 seconds per metadata+PNG job. Unauthenticated render
was rejected with 401, palette used at most 16 colors and the API was stopped.
See [HTTP record](openfreemap/http-smoke.json); it is not the Garmin bridge.

## Historical checks and failures

The earlier synthetic grid experiment (18:49:58–19:12:03 UTC) displayed all six
raster profiles via temporary HTTPS/Garmin conversion. During an API outage GPS
advanced 360→485, raster stayed intact and trace remained 180; automatic recovery
advanced Images 12→13. A 1024-character Storage round trip passed. Earlier large
Storage experimentation failed with OOM; capacity remains unknown. The tunnel was
closed and private configuration restored. See [historical record](previous-synthetic-simulator.json).
Those tiny grid PNGs and older Turkish screenshots are not current real map evidence.

Corrections in the new extension: compressed cache eviction byte accounting;
separate render/image locks; explicit provider failure without synthetic fallback;
palette RGB channel conversion; English labels shortened after round width failures;
GPS coordinates accepted outside Mercator coverage. All corresponding regressions pass.
Earlier CIQ coordinate precision, stale Properties, image success counter and local
PNG conversion findings remain documented in [ADR 003](../decisions/003-simulator-findings.md).

## Records and limits

[Automated commands](automated.json), [21-test output](openfreemap/watch-tests-final.txt),
[GUI evidence](simulator.json), [provider profiles](openfreemap/provider-profiles.json),
[city samples](openfreemap/provider-cities.json), [review](openfreemap/review.md),
[package verification](source-package.json).

`FR165_PUBLIC_BASE_URL` must point to the user's HTTPS converter for watch maps.
Localhost works only for local API development. No permanent hosting was created.
The renewed public HTTPS test was authorized and began at 20:58:42 UTC with a
21:28:42 UTC deadline. Initial physical map display FAILED; the corrected build
displays the map without crashing according to the user. See [physical HTTPS evidence](physical-https.json) and
[native raster decision](../decisions/005-fr165-native-raster.md).
Full physical acceptance is not passed. Source ZIP tests reuse the isolated
project `.venv`; they do not claim another clean dependency installation.

## Physical USB installation

The later [installation record](physical-install.json) verifies model/part, device
software version and PRG transfer plus byte for byte readback. It does not prove
app launch, satellite GPS, iPhone networking or G0 acceptance. No personal
location or device identifier was committed.

The physical diagnostic photo shows Phone YES, Images 0, GPS 44, Trace 11 and
27/27 KiB displayed app heap/peak with network code -400 (invalid response body).
Storage probe was NOT RUN. This is a no image session sample, not loaded map memory
or a leak/endurance measurement. Raw photo is excluded; sanitized observations are
in [physical HTTPS evidence](physical-https.json).

The compact attribution follow up uses a 213 x 18 px text line in a 225 x 24 px
band, with no packaged font. Day/night maps and credits were visually checked
using a private simulator fixture with fixed sample data and no network/GPS
subscription. This UI change is locally verified, not yet installed physically.
See [current UI evidence](compact-attribution/result.json).
