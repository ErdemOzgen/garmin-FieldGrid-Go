# Portal status

The current FieldGrid application has no account, pairing or web portal. It does
not send data to a phone or server. This directory remains from the earlier G2
portal proposal. The historical API's `/docs` page is developer documentation,
not a Bluetooth bridge or a user portal. No open iPhone browser tab is required.
