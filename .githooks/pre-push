#!/bin/sh
set -eu
cd "$(git rev-parse --show-toplevel)"
python=.venv/bin/python
if [ ! -x "$python" ]; then python=python3; fi
"$python" scripts/check_secrets.py
exec "$python" scripts/check_secrets.py --history
